namespace Task4UTests
{
    [TestClass]
    public class DesksViewModelTests
    {
        [TestMethod]
        public void DesksAddedAndRemoved()
        {
            // Add first desk
            //var deskViewModel1 = new DeskViewModel { Name = "desk1" };

            //_desksViewModel.NewTaskViewModel = deskViewModel1;
            //_desksViewModel.AddTaskCommand.Execute(null);

            //Assert.AreEqual(_desksViewModel.Tasks.Count(), 1);
            //Assert.AreEqual("task1", _desksViewModel.Tasks.First().Name);

            //// Add second task
            //var deskViewModel2 = new DeskViewModel { Name = "desk2" };

            //_desksViewModel.AddTaskCdesknd.Execute(null);
            //_desksViewModel.NewTaskViewModel = deskViewModel2;

            //Assert.AreEqual(2, _desksViewModel.Tasks.Count());
            //Assert.AreEqual("desk2", _desksViewModel.Tasks.Skip(1).First().Name);

            //// Remove first task
            //_desksViewModel.Tasks.First().IsSelected = true;

            //_desksViewModel.RemoveSelectedTasksCommand.Execute(null);

            //Assert.AreEqual(1, _desksViewModel.Tasks.Count());
            //Assert.AreEqual("desk2", _desksViewModel.Tasks.First().Name);
        }
    }
}
