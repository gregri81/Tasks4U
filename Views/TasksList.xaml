<UserControl x:Class="Tasks4U.Views.TasksList"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Tasks4U.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <DataGrid DataContext="{Binding Tasks}">
        <DataGrid.Columns>
            <DataGridTextColumn Header="Name"/>
            <DataGridTextColumn Header="Description"/>
            <DataGridComboBoxColumn Header="Desk" />
            <DataGridComboBoxColumn Header="Date" />
            <DataGridComboBoxColumn Header="Frequency" />
        </DataGrid.Columns>
    </DataGrid>
</UserControl>
